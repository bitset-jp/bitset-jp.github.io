import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  output: 'export',
  basePath: '/bitset-jp.github.io',
  assetPrefix: '/bitset-jp.github.io/',
};

export default nextConfig;
